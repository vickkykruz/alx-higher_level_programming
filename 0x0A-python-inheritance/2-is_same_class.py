#!/usr/bin/python3
"""
    This is ``is_same_class`` that checks if the object is exactly an
    instance of the specified class
"""


def is_same_class(obj, a_class):
    """ This is a method that return True or False """

    return (type(obj) == a_class)
